import { sellers, createSeller } from './utils';

const init = () => {
  const appRunning = true;

  while (appRunning) {

  }
  // while user doesn't exit app

  // inquire about user type
  // ask seller flow from all 4
  // perform error handling or success operations
};

init();
